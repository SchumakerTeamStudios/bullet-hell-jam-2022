name: Nswitch

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  my_job:
    container: devkitpro/devkita64

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
  
